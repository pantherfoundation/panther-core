{
  "name": "@panther-core/subgraph",
  "license": "SEE LICENSE IN ./LICENSE",
  "version": "0.0.1",
  "scripts": {
    "codegen": "graph codegen",
    "auth": "graph auth --product hosted-service",
    "build": "graph build",
    "deploy:hosted": "graph deploy --node https://api.thegraph.com/deploy/",
    "deploy:studio": "graph deploy --studio",
    "subgraph:publish": "yarn codegen && yarn build && yarn deploy:studio"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.55.0",
    "@graphprotocol/graph-ts": "0.31.0"
  },
  "devDependencies": {
    "dotenv": "^16.3.1"
  }
}
